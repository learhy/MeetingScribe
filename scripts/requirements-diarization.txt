# Speaker Diarization Requirements for MeetingScribe
# Install with: pip install -r requirements-diarization.txt

# Core dependencies
# Pin to stable versions - PyTorch 2.9+ has compatibility issues
torch>=2.0.0,<2.9.0
torchaudio>=2.0.0,<2.9.0
openai-whisper>=20231117

# Fast diarization with SpeechBrain
speechbrain>=1.0.0
scikit-learn>=1.0.0

# Audio backend for standalone Python (soundfile provides audio loading)
soundfile>=0.12.0

# HuggingFace Hub - pin to <1.0 for speechbrain 1.0.x compatibility
huggingface-hub>=0.19.0,<1.0

# Audio processing
numpy>=1.20.0
