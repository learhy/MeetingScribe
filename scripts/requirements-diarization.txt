# Speaker Diarization Requirements for MeetingScribe
# Install with: pip install -r requirements-diarization.txt

# Core dependencies
# Pin to stable versions - PyTorch 2.9+ has venv compatibility issues
torch>=2.0.0,<2.9.0
torchaudio>=2.0.0,<2.9.0
openai-whisper>=20231117

# Fast diarization with SpeechBrain
speechbrain>=1.0.0
scikit-learn>=1.0.0

# Audio processing
numpy>=1.20.0
